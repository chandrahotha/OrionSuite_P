# Assets

* `logo.png`: product logo, shown at the top of `README.md`.
* `logo_report.png`: report banner, shown in the report preview section.

Both files also ship inside the official npm package `@orionscanner/cli`
under `assets/`, so vendoring these two images here duplicates already
public artwork for GitHub rendering. No binaries and no engine internals
are stored here.
